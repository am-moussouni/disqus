class Friends {
  constructor(db) {
    this.db = db
  }

	// add friendship to database 
	
    // get friends of user from database, returns the record

    // check friendship between 2 users, returns boolean

	// delete friendship
  
exports.default = Friends;